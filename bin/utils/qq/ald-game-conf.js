exports.app_key = ""; //请在此行填写从阿拉丁后台获取的appkey
exports.getLocation = false; //默认不获取用户坐标位置
exports.useOpen = false; //默认不启用，是否启用openid计算，开启后必须上传openid，否则数据不会上报。
exports.openKey = ""; //storage中的openid对应的key, SDK提供了通过读缓存的中openid来上传openid,需要配置缓存中openid对应的key

